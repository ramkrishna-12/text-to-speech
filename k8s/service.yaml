apiVersion: v1
kind: Service
metadata:
  name: tts-backend
  namespace: tts-app
  labels:
    app: tts-backend
spec:
  type: ClusterIP
  selector:
    app: tts-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
